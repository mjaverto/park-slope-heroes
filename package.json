{
  "name": "park-slope-heroes",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "gen:sprites": "node tools/generate-sprites.js",
    "postbuild": "rm -rf dist/assets && cp -R assets dist/assets"
  },
  "devDependencies": {
    "vite": "^8.0.10"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  }
}
